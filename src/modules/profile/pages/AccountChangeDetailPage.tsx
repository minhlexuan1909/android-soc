import React from 'react';
import {View} from 'react-native';
import AccountChangeDetailWrapper from '../components/account/AccountChangeDetailWrapper.native';

const AccountChangeDetailPage = () => {
  return (
    <View style={{flex: 1}}>
      <AccountChangeDetailWrapper />
    </View>
  );
};

export default AccountChangeDetailPage;
