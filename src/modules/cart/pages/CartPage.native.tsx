import React from 'react';
import {View} from 'react-native';
import CartWrapper from '../components/CartWrapper.native';

const CartPage = () => {
  return (
    <View style={{flex: 1}}>
      <CartWrapper />
    </View>
  );
};

export default CartPage;
